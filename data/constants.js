// data/constants.js — 常数数据库
// 所有常用常数已直接集成到计算器键盘按钮中，可通过一键输入使用。
// Constants Library 不再需要独立的常数列表。

window.MSCLab = window.MSCLab || {};

const constants = [
  // 键盘已包含：π, e, N_A, k_B, h, ħ, R, c, e_charge, ε₀, σ_SB, m_e
  // 如需更多常数，请在此添加。
];

window.MSCLab.constantsData = constants;
window.MSCLab.constantCategoryKeys = ["all"];
window.MSCLab.calculatorShortcutAliases = ["pi", "e", "Na", "kB", "h", "hbar", "R", "c", "epsilon0"];
